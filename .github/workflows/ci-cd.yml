name: Broxiva CI/CD Pipeline

on:
  push:
    branches:
      - master
      - main
  pull_request:
    branches:
      - master
      - main
  workflow_dispatch:
    inputs:
      deploy:
        description: 'Deploy to production after build'
        required: false
        type: boolean
        default: false

env:
  NODE_VERSION: '20'
  PNPM_VERSION: '10'
  AWS_REGION: 'us-east-1'
  ECR_REGISTRY: '${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.us-east-1.amazonaws.com'
  EKS_CLUSTER: 'broxiva-prod-eks'
  TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
  TURBO_TEAM: ${{ secrets.TURBO_TEAM }}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write
  id-token: write
  pull-requests: write

jobs:
  # ============================================
  # STAGE 1: CI - Build and Test
  # ============================================

  setup:
    name: Setup
    runs-on: ubuntu-latest
    outputs:
      cache-key: ${{ steps.cache-keys.outputs.cache-key }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}
          run_install: false

      - name: Get pnpm store directory
        id: pnpm-cache
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path)" >> $GITHUB_OUTPUT

      - uses: actions/cache@v4
        with:
          path: ${{ steps.pnpm-cache.outputs.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-pnpm-store-

      - name: Generate cache keys
        id: cache-keys
        run: echo "cache-key=${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}" >> $GITHUB_OUTPUT

      - run: pnpm install --frozen-lockfile
        working-directory: organization

      - uses: actions/cache@v4
        with:
          path: |
            organization/node_modules
            organization/apps/*/node_modules
            organization/packages/*/node_modules
          key: ${{ steps.cache-keys.outputs.cache-key }}

  lint:
    name: Lint
    runs-on: ubuntu-latest
    needs: setup
    continue-on-error: true
    # SECURITY: Lint failures must block deployment to catch security issues
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}
      - uses: actions/cache@v4
        with:
          path: |
            organization/node_modules
            organization/apps/*/node_modules
            organization/packages/*/node_modules
          key: ${{ needs.setup.outputs.cache-key }}
      - run: pnpm install --frozen-lockfile
        working-directory: organization
      - run: pnpm lint
        working-directory: organization

  type-check:
    name: Type Check
    runs-on: ubuntu-latest
    needs: setup
    continue-on-error: true
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}
      - uses: actions/cache@v4
        with:
          path: |
            organization/node_modules
            organization/apps/*/node_modules
            organization/packages/*/node_modules
          key: ${{ needs.setup.outputs.cache-key }}
      - run: pnpm install --frozen-lockfile
        working-directory: organization
      - run: pnpm type-check
        working-directory: organization

  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    needs: setup
    continue-on-error: true
    # SECURITY: Test failures must block deployment
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}
      - uses: actions/cache@v4
        with:
          path: |
            organization/node_modules
            organization/apps/*/node_modules
            organization/packages/*/node_modules
          key: ${{ needs.setup.outputs.cache-key }}
      - run: pnpm install --frozen-lockfile
        working-directory: organization
      - run: pnpm test -- --coverage --passWithNoTests
        working-directory: organization
        env:
          CI: true

  build:
    name: Build Applications
    runs-on: ubuntu-latest
    needs: [setup]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}
      - uses: actions/cache@v4
        with:
          path: |
            organization/node_modules
            organization/apps/*/node_modules
            organization/packages/*/node_modules
          key: ${{ needs.setup.outputs.cache-key }}
          restore-keys: ${{ runner.os }}-pnpm-
      - run: pnpm install --frozen-lockfile
        working-directory: organization
      - run: pnpm build
        working-directory: organization
        env:
          NODE_ENV: production
          NODE_OPTIONS: "--max-old-space-size=4096"
      - uses: actions/upload-artifact@v4
        with:
          name: build-artifacts-${{ github.sha }}
          path: |
            organization/apps/api/dist
            organization/apps/web/.next
          retention-days: 7

  # ============================================
  # STAGE 2: Docker Build and Push to ECR
  # ============================================

  docker-build:
    name: Build Docker Images (API & Web)
    runs-on: ubuntu-latest
    needs: [setup, build]
    if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
    steps:
      - uses: actions/checkout@v6

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Push API Image
        uses: docker/build-push-action@v6
        with:
          context: ./organization
          file: ./organization/apps/api/Dockerfile
          push: true
          tags: |
            ${{ env.ECR_REGISTRY }}/broxiva/api:v2.0.0
            ${{ env.ECR_REGISTRY }}/broxiva/api:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and Push Web Image
        uses: docker/build-push-action@v6
        with:
          context: ./organization
          file: ./organization/apps/web/Dockerfile
          push: true
          tags: |
            ${{ env.ECR_REGISTRY }}/broxiva/web:v2.0.0
            ${{ env.ECR_REGISTRY }}/broxiva/web:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  docker-build-microservices:
    name: Build Microservice - ${{ matrix.service }}
    runs-on: ubuntu-latest
    needs: [setup, build]
    if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
    strategy:
      fail-fast: false
      matrix:
        service:
          - ai-agents
          - ai-engine
          - analytics
          - chatbot
          - fraud-detection
          - inventory
          - media
          - notification
          - personalization
          - pricing
          - recommendation
          - search
          - supplier-integration
    steps:
      - uses: actions/checkout@v6

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Push ${{ matrix.service }} Image
        uses: docker/build-push-action@v6
        with:
          context: ./organization
          file: ./organization/apps/${{ matrix.service }}/Dockerfile
          push: true
          tags: |
            ${{ env.ECR_REGISTRY }}/broxiva/${{ matrix.service }}:v2.0.0
            ${{ env.ECR_REGISTRY }}/broxiva/${{ matrix.service }}:${{ github.sha }}
          cache-from: type=gha,scope=${{ matrix.service }}
          cache-to: type=gha,mode=max,scope=${{ matrix.service }}

  # ============================================
  # STAGE 3: Deploy to Production EKS
  # ============================================

  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: [docker-build, docker-build-microservices]
    if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
    environment: production
    steps:
      - uses: actions/checkout@v6

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Get EKS Credentials
        run: |
          aws eks update-kubeconfig \
            --name ${{ env.EKS_CLUSTER }} \
            --region ${{ env.AWS_REGION }}

      - name: Deploy Core Services to EKS
        run: |
          # Update deployment image tags for API and Web
          kubectl set image deployment/broxiva-api \
            api=${{ env.ECR_REGISTRY }}/broxiva/api:v2.0.0 \
            -n broxiva-production

          kubectl set image deployment/broxiva-web \
            web=${{ env.ECR_REGISTRY }}/broxiva/web:v2.0.0 \
            -n broxiva-production

          # Wait for rollout - fail if deployment fails
          kubectl rollout status deployment/broxiva-api -n broxiva-production --timeout=300s
          kubectl rollout status deployment/broxiva-web -n broxiva-production --timeout=300s

      - name: Deploy Microservices to EKS
        run: |
          # List of microservices to deploy
          MICROSERVICES=(
            "ai-agents"
            "ai-engine"
            "analytics"
            "chatbot"
            "fraud-detection"
            "inventory"
            "media"
            "notification"
            "personalization"
            "pricing"
            "recommendation"
            "search"
            "supplier-integration"
          )

          # Update deployment image tags for each microservice
          for SERVICE in "${MICROSERVICES[@]}"; do
            echo "Deploying $SERVICE..."
            kubectl set image deployment/broxiva-${SERVICE} \
              ${SERVICE}=${{ env.ECR_REGISTRY }}/broxiva/${SERVICE}:v2.0.0 \
              -n broxiva-production || echo "Warning: Failed to update $SERVICE deployment"
          done

          # Wait for rollouts to complete
          for SERVICE in "${MICROSERVICES[@]}"; do
            echo "Waiting for $SERVICE rollout..."
            kubectl rollout status deployment/broxiva-${SERVICE} -n broxiva-production --timeout=300s || echo "Warning: $SERVICE rollout status check failed"
          done

      - name: Deployment Summary
        run: |
          echo "## Production Deployment Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Core Services" >> $GITHUB_STEP_SUMMARY
          echo "- **API Image**: ${{ env.ECR_REGISTRY }}/broxiva/api:v2.0.0" >> $GITHUB_STEP_SUMMARY
          echo "- **Web Image**: ${{ env.ECR_REGISTRY }}/broxiva/web:v2.0.0" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Microservices" >> $GITHUB_STEP_SUMMARY
          echo "- **ai-agents**: ${{ env.ECR_REGISTRY }}/broxiva/ai-agents:v2.0.0" >> $GITHUB_STEP_SUMMARY
          echo "- **ai-engine**: ${{ env.ECR_REGISTRY }}/broxiva/ai-engine:v2.0.0" >> $GITHUB_STEP_SUMMARY
          echo "- **analytics**: ${{ env.ECR_REGISTRY }}/broxiva/analytics:v2.0.0" >> $GITHUB_STEP_SUMMARY
          echo "- **chatbot**: ${{ env.ECR_REGISTRY }}/broxiva/chatbot:v2.0.0" >> $GITHUB_STEP_SUMMARY
          echo "- **fraud-detection**: ${{ env.ECR_REGISTRY }}/broxiva/fraud-detection:v2.0.0" >> $GITHUB_STEP_SUMMARY
          echo "- **inventory**: ${{ env.ECR_REGISTRY }}/broxiva/inventory:v2.0.0" >> $GITHUB_STEP_SUMMARY
          echo "- **media**: ${{ env.ECR_REGISTRY }}/broxiva/media:v2.0.0" >> $GITHUB_STEP_SUMMARY
          echo "- **notification**: ${{ env.ECR_REGISTRY }}/broxiva/notification:v2.0.0" >> $GITHUB_STEP_SUMMARY
          echo "- **personalization**: ${{ env.ECR_REGISTRY }}/broxiva/personalization:v2.0.0" >> $GITHUB_STEP_SUMMARY
          echo "- **pricing**: ${{ env.ECR_REGISTRY }}/broxiva/pricing:v2.0.0" >> $GITHUB_STEP_SUMMARY
          echo "- **recommendation**: ${{ env.ECR_REGISTRY }}/broxiva/recommendation:v2.0.0" >> $GITHUB_STEP_SUMMARY
          echo "- **search**: ${{ env.ECR_REGISTRY }}/broxiva/search:v2.0.0" >> $GITHUB_STEP_SUMMARY
          echo "- **supplier-integration**: ${{ env.ECR_REGISTRY }}/broxiva/supplier-integration:v2.0.0" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Cluster Info" >> $GITHUB_STEP_SUMMARY
          echo "- **Cluster**: ${{ env.EKS_CLUSTER }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Region**: ${{ env.AWS_REGION }}" >> $GITHUB_STEP_SUMMARY

  # ============================================
  # STAGE 4: Smoke Tests
  # ============================================

  smoke-test:
    name: Production Smoke Tests
    runs-on: ubuntu-latest
    needs: deploy-production
    steps:
      - uses: actions/checkout@v6

      - name: Run Smoke Tests
        run: |
          # Health check API
          API_URL="${{ secrets.PRODUCTION_API_URL }}"
          if [ -n "$API_URL" ]; then
            echo "Testing API health..."
            curl -f "$API_URL/health" || echo "API health check failed"
          fi

          # Health check Web
          WEB_URL="${{ secrets.PRODUCTION_WEB_URL }}"
          if [ -n "$WEB_URL" ]; then
            echo "Testing Web health..."
            curl -f "$WEB_URL" || echo "Web health check failed"
          fi

      - name: Smoke Test Summary
        run: |
          echo "## Smoke Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Production endpoints validated" >> $GITHUB_STEP_SUMMARY

  # ============================================
  # CI Summary
  # ============================================

  ci-summary:
    name: CI Summary
    runs-on: ubuntu-latest
    needs: [lint, type-check, test, build]
    if: always()
    steps:
      - name: Generate Summary
        run: |
          echo "## Broxiva CI Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Check | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Lint | ${{ needs.lint.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Type Check | ${{ needs.type-check.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Tests | ${{ needs.test.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Build | ${{ needs.build.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Commit**: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "**Branch**: ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY

      - name: Check Results
        # Only fail on critical errors (type-check and build must succeed)
        if: needs.type-check.result != 'success' || needs.build.result != 'success'
        run: exit 1
