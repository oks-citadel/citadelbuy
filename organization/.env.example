# Broxiva E-Commerce Platform - Environment Configuration
# Copy this file to .env and fill in your values

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# CRITICAL: Replace with a secure password!
# Generate with: openssl rand -base64 32
# NEVER use weak passwords like "password123" or "changeme"
POSTGRES_USER=broxiva
POSTGRES_PASSWORD=your_secure_password_here_min_32_chars
POSTGRES_DB=broxiva_dev
DATABASE_URL=postgresql://broxiva:your_secure_password_here_min_32_chars@localhost:5432/broxiva_dev

# ===========================================
# REDIS CONFIGURATION
# ===========================================
REDIS_URL=redis://localhost:6379

# ===========================================
# JWT AUTHENTICATION
# ===========================================
# CRITICAL: Generate secure JWT secrets!
# Generate with: openssl rand -base64 64
# Minimum 64 characters recommended for production
# MUST be different for JWT_SECRET and JWT_REFRESH_SECRET
JWT_SECRET=your_jwt_secret_key_minimum_64_characters_openssl_rand_base64_64
JWT_REFRESH_SECRET=your_jwt_refresh_secret_different_from_jwt_secret_64_chars
JWT_EXPIRATION=1h
JWT_REFRESH_EXPIRATION=7d

# ===========================================
# SERVER CONFIGURATION
# ===========================================
NODE_ENV=development
PORT=4000
CORS_ORIGIN=http://localhost:3000

# ===========================================
# STRIPE PAYMENT GATEWAY
# ===========================================
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# ===========================================
# FRONTEND CONFIGURATION
# ===========================================
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# ===========================================
# AWS SERVICES (SNS, SES, SQS) - REQUIRED
# ===========================================
# NOTE: External email/SMS providers (SendGrid, Twilio, etc.) are NOT supported.
# All messaging must use AWS services (SES, SNS, SQS) per infrastructure policy.
# Shared AWS credentials (used by SNS, SES, SQS if specific keys not set)
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=us-east-1

# AWS SNS (SMS) Configuration
AWS_SNS_ACCESS_KEY_ID=your_aws_sns_access_key
AWS_SNS_SECRET_ACCESS_KEY=your_aws_sns_secret_key
AWS_SNS_REGION=us-east-1
AWS_SNS_SENDER_ID=Broxiva
AWS_SNS_DEFAULT_SMS_TYPE=Transactional

# AWS SES (Email) Configuration
AWS_SES_ACCESS_KEY_ID=your_aws_ses_access_key
AWS_SES_SECRET_ACCESS_KEY=your_aws_ses_secret_key
AWS_SES_REGION=us-east-1
AWS_SES_FROM_EMAIL=noreply@broxiva.com
AWS_SES_FROM_NAME=Broxiva
AWS_SES_CONFIGURATION_SET=broxiva-emails

# AWS SQS (Message Queue) Configuration
AWS_SQS_ACCESS_KEY_ID=your_aws_sqs_access_key
AWS_SQS_SECRET_ACCESS_KEY=your_aws_sqs_secret_key
AWS_SQS_REGION=us-east-1
AWS_SQS_QUEUE_NAME=broxiva-notifications
AWS_SQS_QUEUE_URL=https://sqs.us-east-1.amazonaws.com/123456789/broxiva-notifications
AWS_SQS_DLQ_URL=https://sqs.us-east-1.amazonaws.com/123456789/broxiva-notifications-dlq

# ===========================================
# ELASTICSEARCH (OPTIONAL)
# ===========================================
ELASTICSEARCH_URL=http://localhost:9200
ELASTICSEARCH_INDEX=broxiva-products

# ===========================================
# MONITORING & LOGGING
# ===========================================
LOG_LEVEL=debug
SENTRY_DSN=your_sentry_dsn_for_error_tracking

# ===========================================
# ADMIN TOOLS PASSWORDS
# ===========================================
# Required for Docker Compose stacks that include admin tools
# Generate with: openssl rand -base64 32
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=your_secure_grafana_password_here
PGADMIN_DEFAULT_EMAIL=admin@broxiva.com
PGADMIN_DEFAULT_PASSWORD=your_secure_pgadmin_password_here

# ===========================================
# MESSAGE QUEUE
# ===========================================
# Required for RabbitMQ service
RABBITMQ_USER=broxiva
RABBITMQ_PASSWORD=your_secure_rabbitmq_password_here

# ===========================================
# STORAGE SERVICES
# ===========================================
# Required for MinIO service (S3-compatible storage)
MINIO_ROOT_USER=broxiva_admin
MINIO_ROOT_PASSWORD=your_secure_minio_password_min_8_chars

# ===========================================
# AI SERVICES (OPTIONAL)
# ===========================================
OPENAI_API_KEY=sk-your_openai_api_key
TENSORFLOW_MODEL_PATH=./models

# ===========================================
# SECURITY NOTES
# ===========================================
# 1. NEVER commit the actual .env file to version control
# 2. Use different passwords for dev, staging, and production
# 3. Rotate passwords regularly in production environments
# 4. Store production secrets in a secrets manager (AWS Secrets Manager, HashiCorp Vault, etc.)
# 5. All passwords should be at least 32 characters long
# 6. JWT secrets should be at least 64 characters long
# 7. Use the provided openssl commands to generate secure random passwords
