apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "commerce-service.configMapName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "commerce-service.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.configMap }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
