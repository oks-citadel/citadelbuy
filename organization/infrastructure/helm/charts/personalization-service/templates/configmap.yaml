apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "personalization-service.configMapName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "personalization-service.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.configMap }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
