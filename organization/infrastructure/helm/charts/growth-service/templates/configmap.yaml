apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "growth-service.configMapName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "growth-service.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.configMap }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
