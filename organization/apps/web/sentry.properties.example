# Sentry Configuration File for Broxiva Web Frontend
# ========================================================
# This file configures the Sentry CLI for uploading source maps and creating releases.
# Copy this file to sentry.properties and update with your actual values.
#
# IMPORTANT: Do NOT commit sentry.properties with actual values to version control!
# The auth token should come from environment variables in CI/CD.

# Sentry API endpoint
defaults.url=https://sentry.io/

# Your Sentry organization slug
# Get this from: https://sentry.io/settings/
# Example: broxiva
defaults.org=your-organization-slug

# Your Sentry project slug for the web frontend
# Get this from: https://sentry.io/settings/projects/
# Example: broxiva-web-prod
defaults.project=broxiva-web-prod

# Authentication token for uploading source maps
# Get from: https://sentry.io/settings/account/api/auth-tokens/
# Permissions needed: project:releases, project:write
#
# SECURITY: This token should come from environment variable in production
# The ${SENTRY_AUTH_TOKEN} syntax reads from the environment variable
# Never hardcode the actual token here!
auth.token=${SENTRY_AUTH_TOKEN}

# Optional: Specify the Sentry CLI version
# cli.executable=/path/to/sentry-cli

# Optional: Log level for Sentry CLI
# Options: trace, debug, info, warn, error
# log.level=info
