<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Order Confirmed - CitadelBuy</title>
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; line-height: 1.6; color: #333; margin: 0; padding: 0; background-color: #f5f5f5; }
    .container { max-width: 600px; margin: 0 auto; background: #ffffff; }
    .header { background: linear-gradient(135deg, #27ae60 0%, #2ecc71 100%); padding: 30px 20px; text-align: center; }
    .header h1 { color: #ffffff; margin: 0; font-size: 24px; }
    .header .checkmark { font-size: 48px; margin-bottom: 10px; }
    .content { padding: 30px; }
    .order-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 25px; padding-bottom: 20px; border-bottom: 2px solid #f0f0f0; }
    .order-number { font-size: 14px; color: #666; }
    .order-number strong { font-size: 18px; color: #333; display: block; }
    .order-date { text-align: right; font-size: 14px; color: #666; }
    .greeting { font-size: 18px; margin-bottom: 20px; }
    .message { color: #666; margin-bottom: 25px; }
    .order-items { background: #f8f9fa; border-radius: 8px; padding: 20px; margin-bottom: 25px; }
    .order-items h3 { margin: 0 0 15px; font-size: 16px; border-bottom: 1px solid #e0e0e0; padding-bottom: 10px; }
    .item { display: flex; align-items: center; padding: 12px 0; border-bottom: 1px solid #eee; }
    .item:last-child { border-bottom: none; }
    .item-image { width: 60px; height: 60px; background: #fff; border-radius: 6px; margin-right: 15px; object-fit: cover; border: 1px solid #e0e0e0; }
    .item-details { flex: 1; }
    .item-name { font-weight: 500; margin-bottom: 4px; }
    .item-variant { font-size: 13px; color: #888; }
    .item-price { text-align: right; }
    .item-price .quantity { font-size: 13px; color: #888; }
    .item-price .price { font-weight: 600; }
    .order-summary { background: #fff; border: 1px solid #e0e0e0; border-radius: 8px; padding: 20px; margin-bottom: 25px; }
    .summary-row { display: flex; justify-content: space-between; padding: 8px 0; font-size: 14px; }
    .summary-row.total { font-size: 18px; font-weight: 700; border-top: 2px solid #e0e0e0; margin-top: 10px; padding-top: 15px; }
    .summary-row.discount { color: #27ae60; }
    .addresses { display: flex; gap: 20px; margin-bottom: 25px; }
    .address-box { flex: 1; background: #f8f9fa; border-radius: 8px; padding: 15px; }
    .address-box h4 { margin: 0 0 10px; font-size: 14px; color: #666; }
    .address-box p { margin: 0; font-size: 14px; line-height: 1.5; }
    .delivery-info { background: #e8f5e9; border-radius: 8px; padding: 20px; margin-bottom: 25px; text-align: center; }
    .delivery-info h4 { margin: 0 0 10px; color: #27ae60; }
    .delivery-info p { margin: 0; color: #2e7d32; }
    .cta-button { display: inline-block; background: #667eea; color: #ffffff; padding: 14px 30px; border-radius: 6px; text-decoration: none; font-weight: 600; }
    .help-section { background: #f8f9fa; border-radius: 8px; padding: 20px; margin-top: 25px; }
    .help-section h4 { margin: 0 0 10px; }
    .help-section p { margin: 0; font-size: 14px; color: #666; }
    .help-section a { color: #667eea; text-decoration: none; }
    .footer { background: #2c3e50; padding: 25px; text-align: center; color: #bdc3c7; font-size: 14px; }
    .footer a { color: #ecf0f1; text-decoration: none; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="checkmark">&#10004;&#65039;</div>
      <h1>Order Confirmed!</h1>
    </div>

    <div class="content">
      <div class="order-header">
        <div class="order-number">
          Order Number
          <strong>#{{orderNumber}}</strong>
        </div>
        <div class="order-date">
          {{orderDate}}
        </div>
      </div>

      <div class="greeting">Hi {{customerName}},</div>

      <div class="message">
        <p>Thank you for your order! We've received your purchase and are preparing it for shipment. You'll receive another email with tracking information once your order ships.</p>
      </div>

      <div class="order-items">
        <h3>Order Items ({{itemCount}})</h3>
        {{#each items}}
        <div class="item">
          <img src="{{image}}" alt="{{name}}" class="item-image">
          <div class="item-details">
            <div class="item-name">{{name}}</div>
            {{#if variant}}
            <div class="item-variant">{{variant}}</div>
            {{/if}}
          </div>
          <div class="item-price">
            <div class="quantity">Qty: {{quantity}}</div>
            <div class="price">{{currency}}{{price}}</div>
          </div>
        </div>
        {{/each}}
      </div>

      <div class="order-summary">
        <div class="summary-row">
          <span>Subtotal</span>
          <span>{{currency}}{{subtotal}}</span>
        </div>
        {{#if discount}}
        <div class="summary-row discount">
          <span>Discount ({{discountCode}})</span>
          <span>-{{currency}}{{discount}}</span>
        </div>
        {{/if}}
        <div class="summary-row">
          <span>Shipping</span>
          <span>{{#if shippingFree}}Free{{else}}{{currency}}{{shipping}}{{/if}}</span>
        </div>
        <div class="summary-row">
          <span>Tax</span>
          <span>{{currency}}{{tax}}</span>
        </div>
        <div class="summary-row total">
          <span>Total</span>
          <span>{{currency}}{{total}}</span>
        </div>
      </div>

      <div class="addresses">
        <div class="address-box">
          <h4>&#128230; Shipping Address</h4>
          <p>
            {{shippingAddress.name}}<br>
            {{shippingAddress.line1}}<br>
            {{#if shippingAddress.line2}}{{shippingAddress.line2}}<br>{{/if}}
            {{shippingAddress.city}}, {{shippingAddress.state}} {{shippingAddress.postalCode}}<br>
            {{shippingAddress.country}}
          </p>
        </div>
        <div class="address-box">
          <h4>&#128179; Payment Method</h4>
          <p>
            {{paymentMethod.brand}} ending in {{paymentMethod.last4}}<br>
            {{paymentMethod.email}}
          </p>
        </div>
      </div>

      <div class="delivery-info">
        <h4>&#128666; Estimated Delivery</h4>
        <p>{{estimatedDelivery}}</p>
      </div>

      <div style="text-align: center;">
        <a href="{{trackingUrl}}" class="cta-button">Track Your Order</a>
      </div>

      <div class="help-section">
        <h4>Need Help?</h4>
        <p>
          Have questions about your order? Visit our <a href="{{helpUrl}}">Help Center</a> or contact us at <a href="mailto:{{supportEmail}}">{{supportEmail}}</a>
        </p>
      </div>
    </div>

    <div class="footer">
      <p><strong>CitadelBuy</strong></p>
      <p>Thank you for shopping with us!</p>
      <p style="font-size: 12px; margin-top: 15px;">
        <a href="{{unsubscribeUrl}}">Unsubscribe</a> | <a href="{{privacyUrl}}">Privacy Policy</a>
      </p>
    </div>
  </div>
</body>
</html>
