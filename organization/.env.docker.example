# =========================================================
# Broxiva - Docker Compose Environment Variables
# =========================================================
# CRITICAL SECURITY NOTICE:
# =========================================================
# 1. This is a TEMPLATE file - copy to .env before use
# 2. Replace ALL "CHANGE_ME" values with secure credentials
# 3. NEVER commit the actual .env file to version control
# 4. Use different credentials for dev, staging, and production
# 5. Store production secrets in a secrets manager (AWS Secrets Manager, HashiCorp Vault, etc.)
#
# Quick Start:
#   $ cp .env.docker.example .env
#   $ # Edit .env and replace all CHANGE_ME values
#   $ docker-compose up -d
#
# Security Checklist:
#   [ ] All CHANGE_ME values replaced
#   [ ] Strong passwords generated (minimum 32 characters)
#   [ ] JWT secrets are at least 64 characters
#   [ ] Encryption keys are exactly 64 hex characters
#   [ ] .env file is in .gitignore
#   [ ] Production secrets stored in secrets manager
# =========================================================

# =========================================================
# DATABASE CREDENTIALS
# =========================================================
# SECURITY: Generate strong passwords for all databases
# Command: openssl rand -base64 32
#
# WARNING: Default passwords are NOT secure!
# - NEVER use "password", "123456", "changeme", etc.
# - NEVER reuse passwords across environments
# - Rotate passwords regularly in production
# =========================================================

# PostgreSQL
POSTGRES_USER=broxiva
POSTGRES_PASSWORD=CHANGE_ME_GENERATE_SECURE_PASSWORD_MIN_32_CHARS
POSTGRES_DB=broxiva_dev

# MongoDB (if using docker-compose.production.yml)
MONGO_USER=broxiva
MONGO_PASSWORD=CHANGE_ME_GENERATE_SECURE_PASSWORD_MIN_32_CHARS

# Redis (optional password for production - HIGHLY RECOMMENDED)
REDIS_PASSWORD=CHANGE_ME_GENERATE_SECURE_PASSWORD_MIN_32_CHARS

# =========================================================
# APPLICATION SECRETS
# =========================================================
# CRITICAL: These secrets protect your entire application!
#
# JWT Secrets:
#   - Used to sign authentication tokens
#   - If compromised, attackers can forge authentication
#   - Generate with: openssl rand -base64 64
#   - Minimum 64 characters required
#   - MUST be different for JWT_SECRET and JWT_REFRESH_SECRET
#
# Encryption Keys:
#   - Used to encrypt sensitive data (KYC documents, PII, etc.)
#   - If compromised, encrypted data can be decrypted
#   - Generate with: openssl rand -hex 32
#   - MUST be exactly 64 hexadecimal characters (32 bytes)
#
# WARNING: Changing these in production will:
#   - JWT: Invalidate all user sessions (force re-login)
#   - Encryption: Make existing encrypted data unrecoverable
# =========================================================

# JWT Authentication
# Generate with: openssl rand -base64 64
JWT_SECRET=CHANGE_ME_MUST_BE_AT_LEAST_64_CHARACTERS_LONG_OPENSSL_RAND_BASE64_64
JWT_REFRESH_SECRET=CHANGE_ME_DIFFERENT_FROM_JWT_SECRET_OPENSSL_RAND_BASE64_64

# Encryption Key (32 bytes for AES-256)
# Generate with: openssl rand -hex 32
# MUST be exactly 64 hexadecimal characters
ENCRYPTION_KEY=CHANGE_ME_EXACTLY_64_HEX_CHARACTERS_OPENSSL_RAND_HEX_32

# =========================================================
# ADMIN TOOLS
# =========================================================

# pgAdmin
PGADMIN_DEFAULT_EMAIL=admin@broxiva.com
PGADMIN_DEFAULT_PASSWORD=CHANGE_ME_SECURE_PASSWORD

# Grafana
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=CHANGE_ME_SECURE_PASSWORD

# =========================================================
# STORAGE SERVICES
# =========================================================

# MinIO (S3-compatible storage)
MINIO_ROOT_USER=broxiva_admin
MINIO_ROOT_PASSWORD=CHANGE_ME_MINIMUM_8_CHARACTERS

# =========================================================
# MESSAGE QUEUE
# =========================================================

# RabbitMQ
RABBITMQ_USER=broxiva
RABBITMQ_PASSWORD=CHANGE_ME_SECURE_PASSWORD

# =========================================================
# PAYMENT PROVIDERS (Required for production)
# =========================================================

# Stripe
STRIPE_SECRET_KEY=sk_test_CHANGE_ME
STRIPE_WEBHOOK_SECRET=whsec_CHANGE_ME
NEXT_PUBLIC_STRIPE_PUBLIC_KEY=pk_test_CHANGE_ME

# PayPal
PAYPAL_CLIENT_ID=CHANGE_ME
PAYPAL_CLIENT_SECRET=CHANGE_ME

# =========================================================
# CLOUD SERVICES (Optional)
# =========================================================

# AWS S3
AWS_ACCESS_KEY_ID=CHANGE_ME
AWS_SECRET_ACCESS_KEY=CHANGE_ME
AWS_REGION=us-east-1
AWS_S3_BUCKET=broxiva-uploads

# =========================================================
# SEARCH & AI SERVICES (Optional)
# =========================================================

# Algolia
ALGOLIA_APP_ID=CHANGE_ME
ALGOLIA_API_KEY=CHANGE_ME

# OpenAI
OPENAI_API_KEY=sk-proj-CHANGE_ME

# Anthropic
ANTHROPIC_API_KEY=sk-ant-CHANGE_ME

# =========================================================
# EMAIL SERVICE (Optional)
# =========================================================

EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=noreply@broxiva.com
EMAIL_PASSWORD=CHANGE_ME

# =========================================================
# MONITORING & ANALYTICS (Optional)
# =========================================================

# Sentry
SENTRY_DSN=https://CHANGE_ME@o0000000.ingest.sentry.io/0000000

# Google Analytics
GOOGLE_ANALYTICS_ID=UA-CHANGE_ME

# Facebook Pixel
FACEBOOK_PIXEL_ID=CHANGE_ME
FACEBOOK_ACCESS_TOKEN=CHANGE_ME

# =========================================================
# FRONTEND CONFIGURATION
# =========================================================

NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_WS_URL=ws://localhost:4000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# =========================================================
# DROPSHIPPING SERVICES (if using docker-compose.dropshipping.yml)
# =========================================================

# AliExpress
ALIEXPRESS_APP_KEY=CHANGE_ME
ALIEXPRESS_APP_SECRET=CHANGE_ME

# CJ Dropshipping
CJ_API_KEY=CHANGE_ME
CJ_API_EMAIL=your-email@example.com

# Printful
PRINTFUL_API_KEY=CHANGE_ME

# Spocket
SPOCKET_API_KEY=CHANGE_ME
SPOCKET_API_SECRET=CHANGE_ME

# Zendrop
ZENDROP_API_KEY=CHANGE_ME

# =========================================================
# NOTES:
# =========================================================
# 1. Generate strong passwords using: openssl rand -base64 32
# 2. Generate JWT secrets using: openssl rand -base64 64
# 3. Generate encryption keys using: openssl rand -hex 32
# 4. Keep this file secure and never commit actual .env files
# 5. Use different credentials for dev, staging, and production
# 6. Rotate secrets regularly in production environments
# 7. Consider using a secrets manager (AWS Secrets Manager, HashiCorp Vault, etc.)
# =========================================================
